#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long
#define pii pair<int,int>

using namespace std;

int main() {_
    int t;
    cin >> t;

    while (t--) {
        ll a, b;
        cin >> a >> b;
        cout << 2 << '\n';
        cout << a-1 << ' ' << 1 << '\n';
        cout << a << ' ' << b << '\n';
    }

    return 0;
}